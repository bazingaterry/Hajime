#include "iostream"
#include "cmath"
using namespace std;

int main(int argc, char const *argv[])
{
	int n;
	while(cin >> n && n != 0)
	{
		bool in = 0;
		int m;
		while(cin >> m && m != 0)
		{
			if ()
		}
	}

	return 0;
}
